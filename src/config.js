// 面によらず変わらない値。面ごとに変わるものは stages/ の定義に置く
// （面の長さ COLS は stage.cols、世界の幅は stage.worldW）
export const TAU=Math.PI*2,VIEW_W=640,VIEW_H=480,TILE=40,ROWS=12,HP_MAX=6;
// LIVES_MAX は HUD に並べるハートの数でもある。増やすと表示も追随する
export const LIVES_START=3,LIVES_MAX=5;
