# 柴犬ラン — 作業のしかた

横スクロールアクションゲーム。素のJavaScriptとCanvasのみで、外部ライブラリもビルドツールも使わない。
仕様・遊び方・モジュール構成は [README.md](README.md) を読むこと。

## いちばん大事な決まり

**コードを変えたら必ず検証を通す。**

```bash
node tools/serve.mjs          # 遊ぶ（http://localhost:8765）
node tools/replay-check.mjs   # 挙動が変わっていないか確かめる
```

CIと同じ全シナリオの照合はこれ:

```bash
for s in run boss stage2 pause; do exp=$(grep "^$s=" tools/replay-baseline.txt | cut -d= -f2); EXPECT="$exp" SCENARIO="$s" node tools/replay-check.mjs; done
```

`tools/replay-check.mjs` はスタブDOM上でゲームを自動プレイし、Canvasへの描画呼び出し列をハッシュ化する。
リファクタリングでは**ダイジェストが1文字も変わってはいけない**。変わったら壊している。

ゲームの挙動を**意図的に**変えたときだけ `tools/replay-baseline.txt` を更新する。
更新前に必ずブラウザで意図どおりか確かめること。数字を書き換えて通すのは、検証を捨てるのと同じ。

不一致の原因を追うとき:

```bash
DUMP=/tmp/after.txt node tools/replay-check.mjs
git stash && DUMP=/tmp/before.txt node tools/replay-check.mjs && git stash pop
diff /tmp/before.txt /tmp/after.txt | head
```

`VERBOSE=1` を付けると300フレームごとの区間ハッシュが出る。どのあたりで食い違ったかを絞れる。
CIが落ちたときは実行の成果物（replay-dumps）に描画ログが入っているので、`gh run download` で持ち帰って比較できる。

## 進め方

計画と実行を分ける。多段の作業に着手する前に `plans/YYYYMMDD-<名前>.plan.md` を書き、
判断（何を選び、何をなぜ却下したか）を先に確定させる。過去の計画書が `plans/` にあるので形式はそれに倣う。

作業の単位ごとにコミットする。1コミット＝1つの意味のある変更。コミットメッセージは日本語で、
「何を」だけでなく「なぜ」を書く。

## これまでにやったこと

1. スタンドアロン化（チャット由来のArtifact断片 → 単体で動くHTML）
2. 11モジュールへの分割と識別子の可読化
3. 基盤A: 敵とギミックの登録制（`src/entities/`）
4. 基盤B: 面のデータ化（`src/stages/`）と2面の追加
5. 1面のボスのバランス調整
6. 基盤C: セーブ（`src/save.js`）・面セレクト・ポーズ・ミュート

## 次にやること

**方針: 柴犬ランを完成させて公開する。ゴールは売上ではなく完走。**
判断の経緯は `product/decisions.md`（特に D12）、手順は `plans/20260813-ship-shibarun.plan.md`。

基盤Cのブラウザ確認は2026-08-13に完了した（実ブラウザで17項目、JS例外なし）。

完成の条件は4つだけ。**規模を増やして完成を遠ざけない。**

1. 終わりがある（最終面クリアで、終わったと分かる画面が出る）
2. 初見が最後まで行ける（**開始5秒でHPが6→2まで減るのは高すぎる。実測値**）
3. スマホでまともなレイアウトで遊べる（いま画面の1/3しか使っていない）
4. 公開されている（まず itch.io。費用0円・改修ゼロ・審査なしで置ける）

**完成の条件に入れないもの**（出し終えてからの話）:

- 3面
- ボスの行動パターンを面ごとに作り分ける（いまは名前・体力・位置だけが面ごと）
- 音量調整（いまはオン／オフだけ）
- 16:9対応とポータル（CrazyGames）への提出

## 企画についての調査

`product/` に、市場・競合・配信先・検証手法の調査と、判断の記録がある。
**読むときは `product/README.md` から。** 調査の生データ（`product/research/`）は
全部読まないこと。各文書の冒頭3〜5行に結論がある。

数字の要点だけ: 2Dプラットフォーマーは2025年のSteamでヒット率0.18%（全ジャンル最下位）、
柴犬モチーフのSteam作品は既に10本以上ある。**だからプレイ数や収益を成功指標にしない。**
指標は「公開できたか」の一点（D12）。

## 踏んだ罠（同じ失敗を繰り返さないために）

- **検証していない場所は壊れていても気づけない。** 1面のボスは「毎フレーム timer++ した直後に
  idle で timer-- する」ため一度も行動していなかった。リプレイ検証がボスまで到達していなかったので
  2段階のリファクタリングを素通りした。**面や強敵を足したら、必ずそのシナリオも足す**こと。
- **`Math.sin` などは CPU のアーキテクチャで最終1ビットが変わる。** arm64のMacとx64のLinuxで
  ダイジェストが割れた。だから座標は小数6桁に丸めて記録している。この丸めを外してはいけない。
- **1回一致したことを不変条件の証明として扱わない。** 「両環境で一致した」と早合点したが、
  たまたま丸めが揃っていただけだった。
- **調査用スクリプトで `step()` だけ回すとカメラが動かない。** `S.cameraX` は `render()` が更新する。
  呼ばないとショットが生成直後に画面外判定で消え、「弾が当たらない」という誤った結論になる。
  ゲームの挙動を測るスクリプトでは `render()` も毎フレーム呼ぶこと。
- **配列を後ろから走査する更新処理は、途中で配列が空にされうる。** 残機を失うと弾が全消しされる。
  `enemyBullet.js` に打ち切りのガードがある。同種のコードを書くときは気をつける。
- **固定ステップの積算（`accum`）に溜まっている端数を捨ててはいけない。** ポーズ中に `accum=0` と
  したら、再開したフレームが `step()` を1回落として以降ずっと1フレームずれた。`dt` は 16.6667
  きっかりにならず、わずかに足りないことがある。端数はその繰り越しなので捨てると1回分が消える。
  `last` は毎フレーム更新しているので、そもそも時間は溜まらずリセットは不要だった。
- **検証ツールが時刻を作っている都合の差を、実装のせいだと思い込まない。** ポーズ中も時刻を進めていたら、
  ゲームが進むフレームに渡る時刻がずれ、`dt = ts - last` の浮動小数の端数の出方が変わって
  たまに1フレームで2回進んだ。ポーズの副作用ではない。時刻を進めるのはゲームが進むフレームだけにした。
- **`run` シナリオが実際に遊んでいるのは5〜795フレームだけ。** 残り2800フレームはゲームオーバー画面。
  何かを「途中で挟む」検証を書くときは、この範囲の外だと黙って素通りする。素通りしたら
  気づけるようにガードを置くこと（`pause` シナリオはそうしてある）。
- **DOMスタブの手抜きは嘘の観測を生む。** `innerHTML` を単なる文字列にしていたため、`innerHTML=''`
  で組み直したはずのボタン一覧が増え続けて見えた。実ブラウザの振る舞いに寄せること。

## 環境について

- npm依存はゼロ。Node があれば動く
- ESモジュールなので `index.html` を直接開いても動かない。必ずサーバ経由
- pushすると GitHub Actions が全シナリオを検証する。落ちたら直すまでマージしない
